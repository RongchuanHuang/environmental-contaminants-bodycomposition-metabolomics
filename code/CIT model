# CIT_example.R
# Example: Causal Inference Test (CIT) for mediation analysis

library(cit)
library(dplyr)
 
# Define your covariates (same set as in LME modeling)
covariates <- c("Age", "BMI", "DietScore", "Gender", "Education",
                "Smoke", "Drink", "Exercise", "Group",
                "Hypertension", "Hyperlipidemia", "Diabetes")

# Run CIT model

   L = data[["PFHxS"]],
   G = data[["M1"]],  # "M1" is the abbreviation of metabolite/lipid
   T = data[["BFM"]],
   C = data[, covariates],

  model <- cit.cp(L_data, G_data, T_data, C_data, rseed = 2025)     
  df_table <- as.data.frame(t(as.data.frame(model))) 

# Run CIT model for all metabolite, and bindrows for the df_table
  CIT_table = tibble()
  df_table <- as.data.frame(t(as.data.frame(model)))  ## 
  CIT_table <- rbind(CIT_table, df_table) 

# FDR Correction
CIT_table_2 = CIT_table %>% mutate(PFDR = p.adjust(p_cit, "BH"))

  

